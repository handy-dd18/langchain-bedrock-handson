langchain>=1.3,<2
langchain-aws>=1.5,<2
langchain-community>=0.4,<0.5
langchain-chroma>=1.1,<2
langchain-text-splitters>=1.1,<2
chromadb>=0.5
python-dotenv>=1.0
boto3>=1.34
ipykernel>=6.29
jupyter>=1.0

# 05章 LangGraph + LangSmith
langgraph>=1.2,<2
langsmith>=0.8.3
grandalf>=0.8  # graph.get_graph().print_ascii() 用 (任意)

# 06章 RAG 深掘り
pypdf>=4.0
beautifulsoup4>=4.12
rank-bm25>=0.2
reportlab>=4.0  # ハンズオン用に PDF を生成するため(本番用途では不要)

# 07章 マルチエージェント
langgraph-supervisor>=0.0.31
# deepagents は langchain 1.x (langchain-core>=1.4) を要求する。
# 依存として langchain-anthropic / langchain-google-genai も入るが、本教材は Bedrock 構成のため API キーは不要。
deepagents>=0.6,<0.7

# 08章 MCP
langchain-mcp-adapters>=0.2,<0.3
mcp>=1.0

# 開発補助: Notebook の実行結果(outputs)を git にコミットしないようにする
# (postCreate で nbstripout --install を実行し、git の clean filter として登録する)
nbstripout>=0.7
